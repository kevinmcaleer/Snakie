.DS_Store
node_modules/
out/
dist/
dist-electron/
*.log
.vite/
.eslintcache

# Claude Code worktrees / local agent state and the local preview helper.
# Track shared project skills (.claude/skills/) but ignore the rest (local config,
# worktrees, scheduled-task locks).
.claude/*
!.claude/skills/
vite.preview.config.mjs

# Python bytecode (plugin SDK/host)
__pycache__/
*.pyc
# TypeScript incremental build cache
*.tsbuildinfo
