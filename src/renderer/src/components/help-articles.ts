/**
 * Help article bodies (markdown), keyed by article id. Authored content for the
 * Help Library document tree ({@link ./help-content}). Rendered by the sanitised
 * {@link ./Markdown} component in the Help panel. A missing id falls back to a
 * "not written yet" placeholder in the panel.
 */
export const HELP_ARTICLES: Record<string, string> = {
  "inst-scope": "A CRT-style oscilloscope that traces a PWM channel as a live square wave.\n\n## What it shows\nDraws the waveform for one PWM pin from its frequency and duty. With a live board it animates the real duty/frequency and shows **FREQ / DUTY / PERIOD**; raw `SNK SCOPE` samples switch it to a sampled trace with **LAST / MIN / MAX**. Use the source selector to pick another PWM pin, and RUN/STOP to hold the trace.\n\n## How to use it\nOpens per PWM pin from the board view. On the board, `import instruments as inst` and call `read_pwm()` on your `machine.PWM` in a loop — it prints `SNK PWM <ch> <freq> <duty>` passively, so it never interrupts a running program.\n\n## Snippet\n```python\nfrom machine import Pin, PWM\nimport instruments as inst\n\npwm = PWM(Pin(0)); pwm.freq(1000)\npwm.duty_u16(32768)\nwhile True:\n    inst.read_pwm(pwm, ch=\"pwm\")\n```",
  "inst-meter": "A skeuomorphic handheld DMM that reads an ADC pin as a voltage.\n\n## What it shows\nA 7-segment LCD voltage, a 0–3.3 V bargraph with the raw 12-bit count, and rolling **MIN / MAX / AVG** stats. Prefers passive `SNK METER` telemetry when present (no raw count then, shown as `----`); otherwise a REPL-polled sample. Pick another ADC pin with the source selector.\n\n## How to use it\nOpens per ADC pin from the board view. On the board, call `read_adc()` on a `machine.ADC` — it converts `read_u16()` to volts against a 3.3 V reference, emits `SNK METER <ch> <volts> V`, and returns the volts.\n\n## Snippet\n```python\nfrom machine import ADC\nimport instruments as inst\n\nadc = ADC(26)\nwhile True:\n    v = inst.read_adc(adc, ch=\"adc0\")\n```",
  "inst-plotter": "A strip-chart recorder that graphs numeric serial output over time.\n\n## What it shows\nA scrolling, auto-scaling line chart on a blue-phosphor screen, with a per-series legend and a `<N> samples · <Hz>` readout. Reads the same serial stream as the terminal (non-disruptively); the metal **CLEAR** key resets the buffer.\n\n## How to use it\nJust `print()` numbers — single values, comma/space/tab rows, or `label:value` pairs all work. For clean named series use `inst.plot()`, which emits a `SNK PLOT` row (only `PLOT` lines are graphed; SCOPE/METER are ignored). Series cap at 16, window at 200 samples.\n\n## Snippet\n```python\nimport instruments as inst\n\nwhile True:\n    inst.plot(temp=21.4, light=80)\n    # or plain: print(temp, light)\n```",
  "inst-range": "A radar/gauge for an ultrasonic or ToF distance sensor.\n\n## What it shows\nAuto-picks a mode from the readings: a **GAUGE** with a needle plus distance-over-time history for a fixed sensor, or a **polar RADAR** sweep with fading trails when readings carry a bearing angle. Configure max range, mm/cm units, and a proximity **ALERT** threshold; out-of-range reads show `NO ECHO`.\n\n## How to use it\nWire an HC-SR04 (TRIG out, ECHO in) and set both in the panel — it live-retargets the board with `SNKCMD range pins`. Your program prints `SNK DIST` via `inst.distance(mm)`. Start the control service so the pin selectors work.\n\n## Snippet\n```python\nimport instruments as inst\n\ninst.start(range_trig=3, range_echo=2)\nwhile True:\n    mm = inst.ranger.read()\n    if mm: inst.distance(mm)\n    inst.control.poll()\n```",
  "inst-imu": "A live 3-D orientation viewer for an accelerometer/gyro IMU.\n\n## What it shows\nA CSS-3D board model that tilts in real time from roll/pitch/yaw or a quaternion, with body axes, an artificial-horizon band, and a numeric **ROLL / PITCH / YAW** readout. **LEVEL** captures the current pose as zero (calibration); **RESET** clears it. The HUD shows `EULER` or `QUAT` per the source.\n\n## How to use it\nRead your IMU (MPU6050, BNO055, …) in a loop and emit orientation. Euler degrees: `inst.imu(roll, pitch, yaw)` → `SNK IMU`. Drift/gimbal-free quaternion: `inst.imu_quat(w, x, y, z)` → `SNK IMUQ`. Both are passive prints, safe inside a tight loop.\n\n## Snippet\n```python\nimport instruments as inst\n\nwhile True:\n    roll, pitch, yaw = read_imu()\n    inst.imu(roll, pitch, yaw)\n    # or: inst.imu_quat(w, x, y, z)\n```",
  "inst-led": "The **write** panel for an LED output — a digital on/off rocker, a PWM brightness slider, an RGB colour picker, and an 8-pixel NeoPixel strip with rainbow/chase animations.\n\n## What it does\nPick a **MODE** (DIGITAL / PWM / RGB / STRIP) and the control writes an IDE→board line `SNKCMD led <payload>` via `sendControl('led', …)`. The glowing bulb reflects the state optimistically (the board doesn't echo LED state back). Payload grammar: `on` / `off`, `pwm <0..1>`, `rgb <r> <g> <b>`, plus `strip …` / `anim …`.\n\n## How to use it\nWire an LED to a GPIO (digital), a PWM pin (dimming), or three PWMs (RGB). On the board, drive an `Led` and route the `led` commands to it.\n\n```python\nfrom instruments import Led\nfrom machine import Pin, PWM\n\nled = Led(pwm=PWM(Pin(15)))\nled.pwm(0.5)      # 50% brightness\n# led.set(True)   # digital on/off\n# led.rgb(255,0,0)\n```\n\n- Sends are fire-and-forget — a disconnected board just no-ops, the UI still updates.",
  "inst-servo": "The **write** panel for a hobby servo (SG90 etc.) — a top-down dial you drag to set the angle, a **SWEEP** that ping-pongs the limits, and MIN/MAX/PIN fields.\n\n## What it does\nDragging the arm (or the slider) writes `SNKCMD servo angle <deg>` via `sendControl('servo', …)`; the **PIN** field writes `pin <n>` (re-attach on GP*n* at 50 Hz) and **DETACH** writes `detach` to drop holding torque. A live `SNK PWM servo …` reading draws a faint MEASURED arm.\n\n## How to use it\nAttach a servo signal wire to a PWM GPIO, then run a program that services the control channel:\n\n```python\nimport instruments as inst, time\n\ninst.start(servo_pin=16)   # attach on GP16 @ 50 Hz\nwhile True:\n    inst.control.poll()    # dial → servo.angle(deg)\n    time.sleep(0.02)\n```\n\n- MIN/MAX cap the travel; the readout shows ANGLE, PULSE (ms) and RANGE.",
  "inst-buzzer": "A music player for a piezo buzzer on a PWM pin — a one-octave piano, an editable melody sequencer, a musical staff, and an RTTTL ringtone box.\n\n## What it does\nClicking a key sounds it (WebAudio in the IDE **and** on the board); **Shift-click** appends it to the melody. ▶ Play streams one compact `buzzer seq …` line; keys/STOP/VOLUME/PIN write `tone <freq> <ms>` / `stop` / `vol` / `pin <n>` via `sendControl('buzzer', …)`. Export/Paste-to-code drop runnable MicroPython.\n\n## How to use it\nPick the **PIN (PWM)**, then run a program that opens the buzzer receiver (▶ Play offers to run a demo if none is live):\n\n```python\nimport instruments as inst, time\n\ninst.start(buzzer_pin=15)   # attach on GP15\nwhile True:\n    inst.control.poll()     # Play → buzzer sounds\n    time.sleep(0.02)\n```\n\n- No board? Keys still preview audibly in the IDE.",
  "inst-sam": "**SAM** — Software Automated Mouth. Type text, pick a pin, and the board speaks it out of a single buzzer/speaker pin.\n\n## What it does\nType into the speech bubble and press **SPEAK** (or Ctrl/Cmd+Enter). The IDE ensures the `sam` library is on the board (mip-installing `github:kevinmcaleer/sam` — with its `sam_render.mpy` accelerator — when missing), then exec-s a `say(...)` on the chosen pin. **Open demo** drops a runnable `sam_demo.py` into the editor.\n\n## How to use it\nConnect a board and choose the **BUZZER PIN** from the board's GPIOs. Unlike the tone panels this runs via `device.exec` — no control-poll loop needed. The exec it sends is simply:\n\n```python\nfrom sam import SAM\nSAM(pin=0).say(\"Hello, I am Sam\")\n```\n\n- Library: github.com/kevinmcaleer/sam",
  "inst-gamepad": "Teleop — drive a connected robot **live** from a physical gamepad (browser Gamepad API) or the on-screen virtual stick + sliders.\n\n## What it does\nEach frame it reads the pad, shapes it through per-output **mappings** (deadzone / scale / trim / invert), and — while driving — streams `{name: value}` axes + pressed buttons at ~25 Hz via `sendControl('teleop', …)`. Safety: **HOLD TO DRIVE** (deadman) only streams while held; **E-STOP** latches every output to zero. Preview works with no robot attached.\n\n## How to use it\nOpen **edit mapping** to name outputs and bind them to gamepad axes/buttons. On the board, read the stream each loop:\n\n```python\nimport instruments as inst, time\n\ninst.start()\nwhile True:\n    inst.control.poll()\n    ax = inst.control.axes(\"teleop\")   # {\"drive\":.5,\"turn\":-.2}\n    fire = inst.control.pressed(\"teleop\", 0)\n    time.sleep(0.02)\n```",
  "inst-button": "Watches a bank of digital inputs live, without touching the REPL.\n\n## What it shows\nOne row per named button/switch with a lit **PRESSED / released** lamp and a **rising-edge counter** (press count). The bottom strip reads **BUTTONS / LAST / EDGES**. It's read-only: the panel has no pins to set — it just parses `SNK BTN <name> <0|1>` from the serial stream.\n\n## How to use it\nWire your button to any `machine.Pin` input, then have your program print a reading each loop with `inst.button(name, state)` (state is coerced to 1 if truthy). The row appears the moment the first line arrives.\n\n```python\nfrom machine import Pin\nimport instruments as inst\n\nbtn = Pin(15, Pin.IN, Pin.PULL_UP)\nwhile True:\n    inst.button(\"A\", not btn.value())  # pull-up: pressed = low\n```",
  "inst-encoder": "A live view of a rotary encoder — a knurled knob that turns to the running count.\n\n## What it shows\nThe knob rotates to the encoder **count**, with a **CW/CCW** direction lamp and a **Δ** step readout. The bottom strip is **COUNT / DIR / RPM** — or **COUNT / DIR / BUTTON** once the encoder reports a push-switch. It reads `SNK ENC <ch> <count> [<pressed>]` non-invasively (last value wins per channel).\n\n## How to use it\nRead your encoder's count (A/B pins) and, optionally, its shaft switch, then emit it each loop with `inst.encoder(count, ch=..., pressed=...)`. Omit `pressed` for an encoder with no button.\n\n```python\nimport instruments as inst\n\ncount = enc.value()          # your encoder's running count\ninst.encoder(count, ch=\"enc\", pressed=sw.value() == 0)\n```",
  "inst-i2c-display": "A skeuomorphic SSD1306 OLED that both **mirrors** and **drives** an I²C display.\n\n## Two modes\n- **Mirror** — renders whatever the board prints as `SNK SCR …` (text rows or a framebuffer), live, with an FPS readout.\n- **Push** — type rows and send them to the real panel over the control channel (`SNKCMD screen text …`).\n\nSDA / SCL / ADDR selectors retarget the display; the bottom strip reads **ADDR / SIZE / FPS**.\n\n## How to use it\nMirror works from any program that calls `inst.screen([...])`. To retarget pins or **Push**, a program must be running and servicing control — start it with the SDA/SCL pins and poll each loop:\n\n```python\nimport instruments as inst\n\ninst.start(screen_sda=0, screen_scl=1)  # I2C0 SSD1306 @ 0x3C\nwhile True:\n    inst.screen([\"Hello,\", \"Snakie!\"])\n    inst.control.poll()\n```\n\nNo program running? The panel offers **Run display demo**.",
  "inst-wifi-scan": "An on-demand Wi-Fi scanner rendered as a signal-bar network list.\n\n## What it does\nPressing **SCAN** clears the list and fires `SNKCMD scan:wifi` on the board; results stream back one `SNK WIFI …` line per network. Each row shows the SSID, a lock icon (secured vs open), 0–4 signal bars and the RSSI in dBm. The readout is **NETWORKS / BEST / BAND** (2.4 vs 5 GHz).\n\n## How to use it\nScanning needs a running Snakie program to service the trigger — `inst.start()` runs the scan (`inst.wifi_scan()`) for you. Poll each loop:\n\n```python\nimport instruments as inst\n\ninst.start()          # registers scan:wifi\nwhile True:\n    inst.control.poll()\n```\n\nWith no program live, **SCAN** offers to open and run the bundled Wi-Fi demo instead.",
  "inst-bluetooth": "An on-demand BLE scanner listing nearby Bluetooth devices by signal strength.\n\n## What it does\n**SCAN** resets the list and sends `SNKCMD scan:bt`; each discovered device arrives as a `SNK BT <name> <mac> <rssi>` line (deduped by MAC, strongest sample kept). Rows show the device **name**, its **MAC** and signal bars + RSSI. The readout is **DEVICES / NEAREST / MODE**.\n\n## How to use it\nA running program must service the trigger — `inst.start()` runs an active `inst.bt_scan()` on request. Poll each loop:\n\n```python\nimport instruments as inst\n\ninst.start()          # registers scan:bt\nwhile True:\n    inst.control.poll()\n```\n\nNo program running? **SCAN** offers to open and run the BLE demo (it scans on the second core).",
  "inst-i2c-detect": "The classic `i2cdetect` 8×16 address grid, as a one-shot dock probe.\n\n## What it does\nPick the **BUS + SDA + SCL** from the connected board's valid I²C pins (invalid combos can't be chosen), then **SCAN** builds a `machine.I2C` on those pins, scans, and lights the responding addresses in the grid. The readout is **FOUND / SDA / SCL**.\n\n## How to use it\nWorks on demand — no running program needed. It runs the probe over the device exec channel, roughly:\n\n```python\nfrom machine import I2C, Pin\nb = I2C(0, sda=Pin(0), scl=Pin(1))\nprint(b.scan())   # -> responding addresses\n```\n\nInside your own program you can emit the same set with `inst.i2c_scan(i2c)` (`SNK I2C …`). A lit cell means a device answered at that address.",
  "gs-connect": "Get Snakie talking to your MicroPython board over USB.\n\n## Plug in & connect\n\n1. **Plug the board in** with a USB data cable (not a charge-only one).\n2. Click **Connect** in the toolbar.\n3. **Pick the serial port** for your board from the list.\n\nOnce connected, the toolbar shows the port and the terminal wakes up.\n\n## The REPL\n\nThe terminal at the bottom is a live MicroPython **REPL** — type Python and press <kbd>Enter</kbd> to run it on the board right away.\n\n- <kbd>Ctrl</kbd>+<kbd>C</kbd> — interrupt a running program\n- <kbd>Ctrl</kbd>+<kbd>D</kbd> — soft reboot (re-runs `main.py`)\n\n## Not showing up?\n\n- Try a different cable or USB port.\n- Close other tools holding the port (Thonny, `mpremote`, a serial monitor).\n- Unplug and replug, then hit **Connect** again.",
  "gs-run": "Write MicroPython in the editor and run it straight on the board.\n\n## Open or create a file\n\n- Open a `.py` from the file tree on the left, **or**\n- Hit <kbd>+</kbd> on the tab strip for a fresh buffer.\n\n```python\nfrom machine import Pin\nfrom time import sleep\n\nled = Pin(\"LED\", Pin.OUT)\nwhile True:\n    led.toggle()\n    sleep(0.5)\n```\n\n## Run & stop\n\n- **Run** executes the current file on the device; output appears in the terminal below.\n- **Stop** interrupts it (same as <kbd>Ctrl</kbd>+<kbd>C</kbd>) — handy for a `while True:` loop.\n\n## Save & upload\n\n**Save & upload** copies the file onto the board so it sticks around. Name it `main.py` to run automatically every time the board boots.",
  "gs-instruments": "Instruments are live gauges — scope, meter, plotter, IMU, range, and more — fed by your program.\n\n## The instrument dock\n\nInstruments live in the **dock** on the right. Use **+ Add instrument** to pick one, and each has **Show/Hide** toggles. On a window, use **Dock to side** / **Float as overlay** to pop it out over the app, and **Close** to dismiss it.\n\n## Feed them from code\n\nCopy `instruments.py` onto the board, then have your loop **print** readings — Snakie parses the serial stream, so it never interrupts a running program.\n\n```python\nimport time\nimport instruments as inst\n\ninst.start(buzzer_pin=15)   # register receivers\n\nwhile True:\n    inst.control.poll()     # service IDE commands + heartbeat\n    inst.scope(value, ch=\"ch1\")\n    inst.meter(1.65, ch=\"adc0\")\n    inst.plot(temp=21.4, light=80)\n    time.sleep(0.05)\n```\n\nOpen the matching instrument, run the program, and it updates live — no LIVE toggle needed.",
  "gs-board-view": "The Board View is a separate, always-on-top window that pictures your wiring.\n\n## Open it\n\nClick the **board** button in the toolbar. It streams live, so the drawing updates as you type — it reads your source for `machine` constructors (`Pin`, `PWM`, `I2C`, `SPI`, `StateMachine`) and wires each used pad to a connection badge.\n\n## Wire up parts\n\nDrag parts from the **library dock** on the right onto the canvas and connect them to the board's pins. A **board selector** in the title bar switches between built-in boards and any you've authored.\n\n## Views\n\nThree tabs across the top:\n\n- **Node graph** — parsed pin usage; the only view with live per-pin values.\n- **Breadboard** — a life-like board + parts you wire by hand.\n- **Schematic** — a wiring diagram.\n\nYour last-used view is remembered.",
  "ref-pins": "Every GPIO is a `machine.Pin`. Set a **direction** (in/out) when you make it, then `.value()` or `.on()/.off()` to use it.\n\n## Output\n\n```python\nfrom machine import Pin\n\nled = Pin(15, Pin.OUT)\nled.on()          # 3.3 V (high)\nled.off()         # 0 V (low)\nled.toggle()\nled.value(1)      # or 0\n```\n\n## Input & pull resistors\n\nA floating input reads noise. Add an internal pull so an unconnected pin has a defined level.\n\n```python\nbtn = Pin(14, Pin.IN, Pin.PULL_UP)\n# wired to GND, so pressed == 0\nif btn.value() == 0:\n    print(\"pressed\")\n```\n\n- `Pin.PULL_UP` — idles **high**, reads 0 when tied to GND\n- `Pin.PULL_DOWN` — idles **low**\n- `Pin(\"LED\", Pin.OUT)` uses the onboard LED on many boards\n\n## Notes\n\n- Pin numbers are the **GPIO** number, not the physical pin. Check the pinout.\n- GPIOs are **3.3 V** — don't feed them 5 V.",
  "ref-timing": "Use the `time` module to pause and to measure elapsed time.\n\n## Delays\n\n```python\nimport time\n\ntime.sleep(1)        # seconds (float ok: 0.25)\ntime.sleep_ms(250)   # milliseconds\ntime.sleep_us(500)   # microseconds\n```\n\n`sleep_ms` / `sleep_us` are MicroPython extras — handy for short, exact pauses.\n\n## Measuring time\n\n`ticks_ms()` is a free-running millisecond counter. Always compare with `ticks_diff` (it handles wrap-around correctly — don't subtract directly).\n\n```python\nstart = time.ticks_ms()\n# ... do work ...\nelapsed = time.ticks_diff(time.ticks_ms(), start)\nprint(elapsed, \"ms\")\n```\n\n## Non-blocking timing\n\nAvoid `sleep` in loops that must stay responsive — poll instead:\n\n```python\nnext_t = time.ticks_ms()\nwhile True:\n    if time.ticks_diff(time.ticks_ms(), next_t) >= 0:\n        next_t = time.ticks_add(next_t, 500)\n        led.toggle()   # every 500 ms\n```",
  "ref-print": "`print()` sends text to the **REPL** — the live terminal below the editor. It's your main tool for seeing what a program is doing.\n\n## Printing\n\n```python\nprint(\"hello\")\nprint(\"temp:\", 21.4, \"C\")      # spaces between args\nprint(f\"pin={pin} val={v}\")    # f-string\nprint(\"loading\", end=\"...\")    # no newline\n```\n\n## The REPL\n\nThe terminal is a live MicroPython prompt — type Python and press Enter to run it now:\n\n```python\n>>> from machine import Pin\n>>> Pin(15, Pin.OUT).on()\n```\n\n## REPL shortcuts\n\n- <kbd>Ctrl</kbd>+<kbd>C</kbd> — interrupt a running program / infinite loop\n- <kbd>Ctrl</kbd>+<kbd>D</kbd> — soft reboot (re-runs `main.py`)\n- `help('modules')` — list every module on the board\n- A bare expression echoes its value: `>>> 2 + 2` → `4`",
  "ref-i2c": "I2C is a **two-wire** bus (SDA + SCL) shared by many devices, each with its own 7-bit address.\n\n## Create the bus\n\n```python\nfrom machine import Pin, I2C\n\n# id/bus number first, then the pins\ni2c = I2C(0, sda=Pin(4), scl=Pin(5), freq=400_000)\n```\n\nThe first argument is the **hardware bus number** (`0` = I2C0, `1` = I2C1). Each bus only allows certain SDA/SCL pins — check your board's pinout. Snakie's board view reads this `id` and labels the pads.\n\n## Scan for devices\n\n```python\nprint([hex(a) for a in i2c.scan()])\n# e.g. ['0x3c', '0x68']\n```\n\nNo addresses? Check wiring, power, and that both lines have **pull-ups** (many breakout boards include them).\n\n## Read / write\n\n```python\ni2c.writeto(0x3c, b\"\\x00\")\ndata = i2c.readfrom(0x68, 6)\ni2c.writeto_mem(0x68, 0x6b, b\"\\x00\")  # register write\n```",
  "ref-spi": "SPI is a fast, **four-wire** bus: SCK (clock), MOSI (out), MISO (in), plus a **CS** (chip-select) per device.\n\n## Create the bus\n\n```python\nfrom machine import Pin, SPI\n\nspi = SPI(0, baudrate=1_000_000,\n          sck=Pin(2), mosi=Pin(3), miso=Pin(4))\ncs = Pin(5, Pin.OUT, value=1)   # idle high\n```\n\nThe first arg is the **bus number** (`0`/`1`); valid SCK/MOSI/MISO pins depend on the board — check the pinout.\n\n## Talk to a device\n\nPull CS **low** for the transaction, then high again:\n\n```python\ncs.value(0)\nspi.write(b\"\\x9f\")        # command\nresp = spi.read(3)        # clock in 3 bytes\ncs.value(1)\n```\n\n## Notes\n\n- Write-only? Skip `miso`. Displays often add a **DC** pin (data/command).\n- Each peripheral needs its **own CS** pin; SCK/MOSI/MISO are shared.",
  "ref-uart": "UART is an async **serial** link: TX out, RX in, both sides set to the same **baudrate**. Great for GPS, modems, and board-to-board links.\n\n## Create the port\n\n```python\nfrom machine import Pin, UART\n\nuart = UART(0, baudrate=9600,\n            tx=Pin(0), rx=Pin(1))\n```\n\nFirst arg is the UART number (`0`/`1`). Cross the wires: your **TX → their RX**, **RX → their TX**, and share **GND**.\n\n## Send & receive\n\n```python\nuart.write(\"AT\\r\\n\")\n\nif uart.any():              # bytes waiting?\n    line = uart.readline()  # or uart.read(n)\n    print(line)\n```\n\n## Notes\n\n- Both ends **must** match baudrate (9600 / 115200 …) or you get garbage.\n- Logic is **3.3 V** — level-shift 5 V devices.\n- `uart.read()` returns `bytes`; decode with `.decode()` for text.",
  "ref-pwm": "PWM switches a pin on/off fast; the **duty cycle** sets the average level. Use it for LED brightness, motor speed, tones, and servos.\n\n## Basics\n\n```python\nfrom machine import Pin, PWM\n\nled = PWM(Pin(15))\nled.freq(1000)            # 1 kHz\nled.duty_u16(32768)       # 0..65535 → ~50%\n```\n\n`duty_u16(0)` = off, `65535` = full on.\n\n## Fade\n\n```python\nfor d in range(0, 65536, 1024):\n    led.duty_u16(d)\n    time.sleep_ms(10)\n```\n\n## Servos\n\nHobby servos want **50 Hz** and a 1–2 ms pulse (0°–180°):\n\n```python\nsg = PWM(Pin(16)); sg.freq(50)\n\ndef angle(deg):            # 0..180\n    us = 500 + deg * 2000 // 180\n    sg.duty_ns(us * 1000)\n\nangle(90)                  # centre\n```\n\nCall `led.deinit()` to release the pin.",
  "ref-pinout": "A **pinout** maps each physical pin to what it does: power, ground, and GPIOs (some doubling as I2C / SPI / UART / ADC). Code always refers to the **GPIO number**, not the physical pin position.\n\n## Reading one\n\n- **GND / 3V3 / VBUS(5V)** — power rails, never a signal\n- **GPn** — general pin, usable as `Pin(n, ...)`\n- **I2C0 SDA/SCL, SPI0 SCK…, UART0 TX/RX** — the bus each pin can join\n- **ADCn** — pins that can read analog voltage\n\nA pin can list several roles, but only one is active at a time.\n\n## In Snakie\n\nOpen the **mini board view** and pick your board from the picker. It draws that board's full pinout and, as you write code, highlights the pads you're actually using and labels their bus role.\n\n```python\n# 'GP15' on the diagram → Pin(15) in code\nled = Pin(15, Pin.OUT)\n```\n\nAlways match the diagram's rails: signals to GPIOs, power to 3V3/5V, and share **GND**.",
}
