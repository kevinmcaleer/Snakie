/**
 * The project's outward-facing URLs, in ONE place (#1109).
 *
 * They are printed on the PDF export's closing page, opened from the status
 * bar's coffee cup, and stamped into exported videos. Two copies of a URL is two
 * things to get wrong, so everything reads them from here.
 */

/** The web build, and the project's home for anyone holding a printout. */
export const SNAKIE_WEB_URL = 'https://app.snakie.org'

/** The bare host, for prose and watermarks where the scheme is just noise. */
export const SNAKIE_WEB_HOST = 'app.snakie.org'

/** How to support Snakie's development (#126). */
export const COFFEE_URL = 'https://buymeacoffee.com/kevinmcaleer'
